const fs = require('fs');
async function test() {
    const text = "みなさんもいま日は無理をせず、自分のペースで、周りに流されずに、マイペースに一日を過ごしてくださいね。";
    const prompt = `あなたは読み仮名修正のプロフェッショナルです。
以下のテキストは音声合成用の「ひらがな台本」ですが、まだ漢字や数字、アルファベットが残っていたり、不自然な誤読（例: 癒やし→ゆやし、朝7時→あさ7とき等）が含まれている可能性があります。
以下のルールに必ず従って、テキストを修正して出力してください。

【超重要ルール】
1. 出力するテキストは【全体を必ずひらがなとカタカナのみの文章】にしてください。漢字は絶対に含めないでください。
2. 残っている漢字・数字・アルファベットは、文脈に合った正しい「ひらがな」または「カタカナ」に変換してください（例: 7 -> しち、なな）。
3. 不自然な誤読（タイポ）があれば、文脈から判断して自然な日本語の読みに修正してください。
4. もとの文章のニュアンス、読点（、）、句点（。）、記号、および[SE: 〇〇]や[ラジオ一時停止: コメント返し]などの特殊タグは【絶対にそのまま残してください】。
5. 修正結果のテキストのみを出力してください。余計な説明や前置きは一切不要です。
6. 出力は元のテキストと同じ行数を保つようにしてください。

【修正対象の台本】
${text}`;

    console.log("Prompt:", prompt);
}
test();
